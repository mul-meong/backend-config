# Local
Server:
  port: 12345
spring:
  application:
    name: config-test-service

 #test priority of ymls
  datasource:
    url: jdbc:test-priority-of-ymls
    username: this will be
    password: overridden
    driver-class-name: com.mysql.cj.jdbc.Driver

# deleted euereka config

# set log level only on this file
logging:
  level:
    root: debug
    org.hibernate.validator: debug
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n'
