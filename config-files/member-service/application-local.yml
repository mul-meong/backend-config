# Local

Server:
  port: ${MEMBER_PORT}

spring:
  application:
    name: member-service
  config:
    import:
      - "configserver:jwt.yml"
      - "configserver:redis.yml"
      - "configserver:kafka-local.yml"
      - "configserver:eureka.yml"
      - "configserver:logging-info.yml"

  datasource:
    url: jdbc:mysql://localhost:3306/member_db
    username: root
    password: 1q2w3e4r
    driver-class-name: com.mysql.cj.jdbc.Driver



